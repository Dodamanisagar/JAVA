package CollectionInterface.Order_1.ListInterface;

public class AboutList {
    /**List:-
     --->List is a predefined interface present in java.util package.
     --->It was introduced from JDK 1.2
     --->The implementation classes of list interface are as follows:
                                                                     1)ArrayList
                                                                     2)Vector
                                                                     3)Stack
                                                                     4)LinkedList
     --->Generalization / upcasting:-
             {new ArrayList();
             {new Vector();
      List l={new Stack();
             {new LinkedList();

     *Specification of List interface:-
     --->Insertion order is Maintained.
     --->Duplication is allowed.
     --->It is Index based and index position starts from zero.
     --->Null values can be inserted.

     *Important methods present in List Interface:-
     1)add(index,value)
     2)set(index,value)
     3)get()
     4)indexOf()
     5)lastIndexOf()


     * */
}
